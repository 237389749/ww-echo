# 声骸评估规则 · 全景说明与审查（eval_rules.md）

> 本文档是判定的**全局视图**：当前实现 → 设计动机 → 分类审查 → 待定边界。
> 代码实现见 `src/task/EnhanceEchoTask.py`（`judge_echo` / `_tier_threshold` / `_legacy_threshold`），
> 演进历史见 `CHANGELOG.md` 阶段八 / 十三 / 十四 / 十五，开发约定见 `CLAUDE.md`。
> 数据依据：`tools/offline_eval_report.py` 在 `logs/eval_debug/20260911_110817`（109 只真实截图）上的重放。

---

## 1. 评分口径

**条分**（每个词条各算一次）：

```
条分 = 档位值 ÷ 该词条均值 × 10 × 权重      每条上限自然 12.5，套装不认的词条记 0 分
总分 = Σ 条分
```

定义两个便于讨论的中间量：

- **档位水平** `r = 档位值 ÷ 均值`：`1.00 = 平均档`、`1.25 = 满档`、`0.75 = 最低档`（各词条档位表 4~8 档不等）
- **Σw = 出现且有效（该套装权重 > 0）的词条权重之和**

**权重表**：通用 `DEFAULT_WEIGHTS`（扮演"配置未定制的默认套装"）

| 词条 | 权重 | | 词条 | 权重 |
|---|---|---|---|---|
| 暴击 | 1.0 | | 共鸣效率 | 0.7 |
| 暴击伤害 | 0.9 | | 一条专伤（普攻/重击/共技/共解） | 0.6 |
| 攻击百分比 | 0.85 | | 固定三系（攻/生/防） | 0.5 |
| 生命/防御百分比 | 0.85 | | | |

> 主C 模型 = 双暴 + 攻击% + 共效 + 一条专伤 + 小攻击 = `1.0 / 0.9 / 0.85 / 0.7 / 0.6 / 0.5`（即通用代表集）。
> 套装模板可在"套装配置"里定制自己的权重集合与取值（决定"认哪些词条"和"每条值多少分"）。

---

## 2. 两条线与五档判定

```
达标线 A = 10 × Σw                        （出现且有效的词条权重之和 ×10）
基准线 B = 该套装有效键权重升序最低 (n−1) 条之和 ×10
           （n = 词条数；配置未定制的套装，其 B 就等于通用线 11 / 18 / 26.5）
```

**判定**（满级 Lv25 = 5 词条；未满级 Lv15/Lv20 = 3/4 词条）：

| 档 | 条件 | 语义 |
|---|---|---|
| **达标** | `score ≥ B` 且 `A ≥ B` 且 `score ≥ A` | 上上之选：现在就能用 |
| **保留** | `score ≥ B`（但不满足上面） | 过线：值得留着 |
| **重铸** | 未过线，但满足胚子信号（见下） | 值得用频整器改造 |
| **待强化** | 未满级 且 `score ≥ B` | 继续练 |
| **不合格** | 其余 | 丢 |

**胚子信号**（"重铸"档的判据）：

```
① 已有 ≥2 条词条达到/超过平均档（档位水平 r ≥ 1.0）     ← 已锁住 ≥2 条好底子
② 四有效打底（Σw 对应的有效条数 ≥ 4）                   ← 只剩 1 条要改
两条满足任一即可
```

> ⚠️ ① 的"好词条"按**档位水平**算、**不看该套装是否认**——因为重铸后套装适配性可以改变。
> 依据 `README` 旧注"单条高档位不保留（洗 4 条不值）"：1 条不够，**2 条起**才值得。

**两个数学等价式**（理解判定的钥匙）：

- `score ≥ A` ⟺ **有效词条的加权平均档位 ≥ 100%**（即"平均达到平均档"）
- `A ≥ B` ⟺ Σw ≥ B/10；**k 太小则 A 必然 < B → 该只永远不能达标**（最高只能"保留"）

---

## 3. 分类审查矩阵

模型 = 主C（双暴/攻%/共效/专伤/小攻击），有效词条取权重高→低的前 k 条，其余权重 0；B = 26.5。

| k | 有效词条（权重） | A | A≥B | 全最低档 75% | 全平均档 100% | 全满档 125% |
|---|---|---|---|---|---|---|
| 1 | 暴击 1.0 | 10.0 | ✗ | 7.5 不合格 | 10.0 不合格 | 12.5 不合格 |
| 2 | 暴击+爆伤 | 19.0 | ✗ | 14.2 不合格 | 19.0 重铸 | 23.8 **重铸** |
| 3 | +攻击% | 27.5 | ✓ | 20.6 不合格 | 27.5 达标 | 34.4 达标 |
| 4 | +共效 | 34.5 | ✓ | 25.9 重铸 | 34.5 达标 | 43.1 达标 |
| 5 | +专伤 | 40.5 | ✓ | 30.4 **保留** | 40.5 达标 | 50.6 达标 |

**过保留线所需的最低平均档位水平**（`score = r × 10 × Σw ≥ 26.5`）：

| k | Σw | 需要 | 可行性 |
|---|---|---|---|
| 1 | 1.00 | 265% | **不可能**（上限 125%） |
| 2 | 1.90 | 139% | **不可能**（上限 125% → 最高 23.75） |
| 3 | 2.75 | 96.4% | 接近平均档才过线 |
| 4 | 3.45 | 76.8% | 最低档（75%）差一点 |
| 5 | 4.05 | 65.4% | 最低档就够 |

**混合档位边界样本**：

```
k=1 暴击满档                  12.50  不合格
k=2 双暴都满档                23.75  重铸      ← 频整器胚子（即使满档也够不到保留线）
k=2 双暴一满一低              19.25  不合格
k=3 双暴满+攻%最低             30.12  达标      ← 2 条满 1 条最低也能达标
k=3 一均两低                  23.12  不合格
k=4 全最低档                  25.88  重铸
k=5 全最低档                  30.38  保留
```

---

## 4. 关键设计选择（为什么这么定）

1. **达标线为什么不是"套装最低 (n−1) 条之和"（旧规则）**
   旧规则只看套装配置的权重分布，与"这只实际出了什么"无关。极端案例：`轻云出月`（9 键，4 个专伤压到 0.15）
   的旧规则线只有 **6.0**、`隐世回光` **5.0**，对比 6 键套装的 26.5~28.5 → **词条种类越多反而越容易达标**。
   现改为"按这只实际出现的有效词条算"。

2. **为什么 A 不取 max(A, B)，而是要求 `A ≥ B` 才可能达标**
   数学上 `k ≥ n−1` 时 `A ≥ B` 恒成立；`k < n−1` 时 A 的求和项少、可能低于 B ——
   那意味着"出现的有效词条太少，不足以支撑达标"，所以这只**最高只能到"保留"**。

3. **为什么不用通用线作额外下限**
   套装配置被定制即为刻意的取向（如 `隐世回光` / `轻云出月` 用低权重"占位键"压低基准线，因为真正核心
   词条少）；若未定制，其 B 本身就等于通用线。用通用线兜底会覆盖作者的意图。

4. **为什么"重铸"看档位水平、不看套装是否认**
   重铸/频整器会改变词条，套装适配性随后可改；所以"是不是好底子"应按**词条本身的档位**判断。
   `双暴满档 + 3 废`（2 条达平均档）正是最好的频整器胚子。

5. **为什么不引入"套装价值系数"**
   "某个套装值不值得练"取决于有没有对应角色/队伍，属于**用户策略**而非可推断的数值；程序口径保持
   "单套装视角"，用户用报告的「套装」下拉筛选来排除不关心的套装即可。
   （附：**"通用分"不能当"散搭收益"**——实测 104 只中 98 只通用分更高，因为通用表覆盖 13 个词条，
   而套装模板只认 5~9 个键；这是口径差异，不是"散搭更强"。）

---

## 5. 待讨论 / 已知边界（**需要拍板的点**）

| # | 现象 | 我的疑问 |
|---|---|---|
| **D1** | `k=5 全最低档`（30.38）→ **保留** | 5 条都在最低档、质量其实很差，但因条数多、总分过线就"保留"。这是"保留 = 过线"的直接结果。**是否要加质量约束**（如"至少 1~2 条达平均档"）？ |
| **D2** | `k=4 全最低档`（25.88）→ **重铸** | 它 4 条都在最低档（要改 4 条），却因 `eff ≥ 4` 进了重铸档。**"四有效打底"是否也要加质量约束**（如"≥1 条达平均档"）？ |
| **D3** | `k=3 双暴满+攻%最低`（30.12）→ **达标** | 只有 2 条好词条，是否算"上上之选"？（数学上它加权平均 109.5% ≥ 100%，按现规则达标） |
| **D4** | `k ≤ 2` 永远无法"保留" | 这是数学结论（`A < B` + `score` 上限）。若认为"双暴满档也该能过线"，则需要**给 A 加下限**或**降低 B**——但那会与"旧规则是下限"的设计冲突。 |
| **D5** | 占位键的双刃性 | 低权重键（0.1~0.15）能压低基准线（辅助套需要的效果），但也会把该套装的 B 拉到 5~6 分，使"几乎任何东西都算过线"。**是否需要约定"权重要么 0、要么 ≥0.5"，或给占位键另设机制**？ |

---

## 6. 未验证 / 风险

- **真机实跑未做**：以上全部基于 `logs/eval_debug/20260911_110817` 的离线重放（详情文本来自
  `image_report.md` 转录，比实时 OCR 干净）。判定逻辑近几轮改动集中，建议跑一次评估核对
  `eval_result.json` 的 `verdict` 分布。
- **档位表版本**：`src/echo_stats.py` 的档位值与游戏版本绑定，改版需同步（否则 r 与 A 都会偏）。
- **权重尺度约定**：套装权重建议保持 ≤1（与通用同尺度）；现有 `隐世回光` 的 `攻击百分比 = 1.7` 会
  让该套装锚线偏高（Lv25 = 36.4）。
- **占位键的基准线副作用**：见 D5。

---

## 7. 附录：当前实现的离线表现（109 只）

| 判定 | 数量 |
|---|---|
| 保留 | 38 |
| 建议重铸 | 22 |
| 达标 | 17 |
| 待强化 | 15 |
| 不合格 | 17 |

各套装贡献（判定分布）：

| 套装 | 达标 | 保留 | 重铸 | 待强化 |
|---|---|---|---|---|
| 长路启航之星 | 1 | 4 | 10 | 5 |
| 隐世回光 | 5 | 7 | 0 | 3 |
| 雪落无声之愿 | 1 | 5 | 7 | 2 |
| 轻云出月 | 5 | 8 | 0 | 2 |
| 流金溯真之式 | 2 | 3 | 8 | 1 |
| 听唤语义之愿 | 0 | 5 | 7 | 0 |
| 息界同调之律 | 2 | 5 | 2 | 1 |

> 注意：`隐世回光` / `轻云出月` 的"重铸"恒为 0 —— 它们的基准线被占位键压到 5~6 分，声骸全部落在"保留"档。
> 这正是"辅助套宽容、主C专属套严格"的效果，且**无需任何套装价值字段**。
