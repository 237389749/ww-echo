# 声骸评估规则 · 全景说明与审查（eval_rules.md）

> 判定的**全局视图**：当前实现 → 设计动机 → 分类审查 → 已评估但不改的项 → 风险。
> 代码：`src/task/EnhanceEchoTask.py`（`judge_echo` / `reforge_plan` / `_tier_threshold` / `_legacy_threshold`）、
> `src/echo_stats.py`（期望 / 分位 / 权重）、`src/echo_score_sim.py`（整只分位）。
> 演进历史见 `CHANGELOG.md` 阶段八 / 十三~十八；开发约定见 `CLAUDE.md`。
> 数据依据：`tools/offline_eval_report.py` 重放 `logs/eval_debug/20260911_110817`（109 只真实截图）。
> **本文已对齐阶段十八口径**（期望归一化 + 边际收益权重 + 分位体系 + 期望分重铸判据）；阶段十五之前的表述均已过时。

---

## 1. 评分口径

**条分**（每个词条各算一次）：

```
条分 = 档位值 ÷ 该词条期望值 × 10 × 权重
总分 = Σ 条分                 （套装不认的词条 = 权重 0 → 记 0 分）
```

**期望值 ≠ 算术平均**：它是官方公示档位概率的加权平均（`assets/echo_probability.json`，**统一舍入 1 位小数**，
报告明细行直接展示它）。官方档位概率并不相等（暴击最低三档各 23.3333%、最高档仅 3%），期望因此低于算术平均
约 10%（双暴）/ 2%~3%（其余）。

- **档位水平** `r = 档位值 ÷ 期望值`：`1.000 = 平均档`；实测区间 **最低档 0.686~0.840 / 满档 1.318~1.400**
- **条分区间**（权重 1.0 时）：最低 **6.9~8.4** / 平均 **10.0** / 满档 **13.1~14.0**
  （旧的算术平均口径下才是 7.5 / 12.5）
- **Σw = 出现且有效（该套装权重 > 0）的词条权重之和**

**权重表**（`DEFAULT_WEIGHTS`；阶段十八按"满档词条的边际收益"重排）：

| 词条 | 权重 | | 词条 | 权重 |
|---|---|---|---|---|
| 暴击 | 1.00 | | 共鸣效率 | 0.60 |
| 暴击伤害 | 0.70 | | 一条专伤（普攻/重击/共技/共解） | 0.40 |
| 攻击% / 生命% / 防御% | 0.50 | | 固定三系（攻/生/防） | 0.25 |

> **定值口径**：覆盖**普通链度/命座**的面板（c≈70% / 爆伤≈200%）。爆伤的相对价值 = `2c/d`，随练度**回落**
> （c70/d200 → 0.70、c65/d250 → 0.52、c90/d300 → 0.60），故通用兜底取 0.70；高练度玩家按角色在套装模板里定制。
> 固定值恒为对应大值的 0.5 倍（比值与面板无关，机制性结论）。共效循环刚需但有阈值、够用即溢出，故 0.60（**这一条是用户口径/体验判断，不是从伤害乘区推导出来的**——与双暴/大攻的边际收益口径不同）。
> **主C 模型（权重降序）= 暴击 1.0 / 爆伤 0.7 / 共效 0.6 / 大攻 0.5 / 专伤 0.4 / 小攻 0.25**
> （注意：共效现在排在攻% 之前——这是与阶段十五之前的显著差异）。
> 套装模板可在"套装配置"里定制自己的权重集合与取值（决定"认哪些词条"和"每条值多少分"）。
> **套装模板是用户手工维护的**（34 套按同一因子折算过一次，之后用户可随时在 UI 里改），
> 因此"某套装某词条的值"以文件为准，不要从本文的倍数关系反推。

---

## 2. 两条线与五档判定

```
达标线 A = 10 × Σw                                  （出现且有效的词条权重之和 ×10）
基准线 B = 该套装有效键权重升序最低 (n−1) 条之和 ×10
           n = 词条数；"通用"模式（套装未映射）用代表集 → 最低 4 条之和 ×10 = 17.5
```

**判定**（满级 Lv25 = 5 词条；未满级 Lv15/Lv20 = 3/4 词条）：

| 档 | 条件 | 语义 |
|---|---|---|
| **达标** | `A ≥ B` 且 `score ≥ A` | 现在就能用 |
| **保留** | `score ≥ B`（其余） | 过线，值得留着 |
| **建议重铸** | 未过 B，但"花频整器**能刷成达标**"（见下） | 值得用频整器改造 |
| **待强化** | 未满级 且 `score ≥ B` | 继续练 |
| **不合格** | 其余 | 丢 |

> `tier ≤ 2`（Lv5/Lv10，词条太少）走**结构判定**：存在首条核心词条（`_core_first`，缺省 = 全部有效词条）即过，
> 文案为「待强化」/「不达标」，不限分。

**"建议重铸"的判据：锁 N 刷 M 后「期望能真正达标」**（`reforge_plan`）

```
穷举 L ∈ {1,2,3,4} 与"锁哪 L 条"的所有组合：
    池 = 13 − L                                    ← 官方公示：锁定的词条类型不会再出现
    E_feat   = 10 × Σw(可用池) ÷ 池                   ← 一条随机新词的期望分
    W_new    = (5−L) × Σw(可用池) ÷ 池                 ← 新刷词条的期望权重和(其档位期望恰为 1.0)
    期望分   = Σ(锁定条的当前条分) + (5−L) × E_feat
    Σw_after = Σw(锁定) + W_new                       ← 重铸后的期望"有效权重和"
    r̄        = [Σ(rᵢ·wᵢ)锁定 + W_new] ÷ Σw_after
    成本     = max(1, L−1) × 30 元                    ← 官方：锁 L 条 = L−1 个频整器(30/个), 锁 1~2 条按 1 个

方案须**依次**通过三道门：
    ① 10 × Σw_after ≥ B    ← 代数粗筛：重铸后的达标线 A_after 自己站得住；否则这只永远"达标"不了
    ② r̄ ≥ 1.0              ← 期望粗筛：等价于"期望分 ≥ A_after"（P ≥ 50% 的方案必过，故不漏）
    ③ 蒙特卡洛达标概率 ≥ **60%**（`REFORGE_MIN_PROB`）← 3000 次模拟真正刷一遍的成败
选法：通过三道门的方案里取**成本最低**（并列取概率最高）
判定：有 → 建议重铸；一个都没有 → 不合格
```

> **为什么还要第三道门（概率）**：①② 都建立在"期望"上，而期望只是均值 —— "期望达标"实际只有约
> 50% 把握，且分数分布右偏（档位有上限、类型无放回）使其更低。而**胚子可以无限刷**：花 30 元只买三成
> 把握，不如去刷新的胚子。实测原 5 只"期望达标"的件，真实概率仅 **21.6% / 23.0% / 34.2% / 42.7% / 51.9%**
> —— 全部 < 60%，因此全部改判"不合格"（这正是用户要的效果）。
>
> **为什么需要两道门**：
> ② 来自"达标线的自适应"——新刷词条的期望档位恰是平均档（1.0），所以 `r̄ ≥ 1.0 ⟺ Σ(rᵢ−1)·wᵢ ≥ 0`，
> 又因 `(5−L)×E_feat = 10×W_new` 两边抵消，它等价于 `期望分 ≥ A_after`。
> **但只有 ② 会漏掉"重铸后的达标线自己就低于基准线"的情形**：例如"锁 1 条低权重词条 + 刷 4 条"，
> `Σw_after` 太小 → `A_after < B` → 按规则最高只能到"保留"，**怎么刷都达不到标**。
> 实测 `梦魔·青羽鹭`：15 个锁定方案里只有 1 个满足 ①（锁 爆伤+攻% → A_after 18.14），而它的 r̄ 0.955
> 又不满足 ② → **任何方案都救不回来，判"不合格"**。加上 ① 之后，通过的都是"锁 2 条好词条、刷 3 条、
> 30 元"这类**只差最后一步**的件（实测 5 只全部如此）。
> 顺带回答了"锁一条废词条有没有用"：没有 —— 那些条 `w = 0`，对 r̄ 无贡献却占一个锁定名额。

**两个数学等价式**（理解判定的钥匙）：

- `score ≥ A` ⟺ **有效词条的加权平均档位 ≥ 100%**（即"平均达到平均档"）
- `A ≥ B` ⟺ `Σw ≥ B/10`；**k（出现的有效词条数）< n−1 时可能 `A < B`** → 该只最高只能到"保留"

---

## 3. 分类审查矩阵

口径：未定制的 **5 键套装**（暴击 1.0 / 爆伤 0.7 / 攻% 0.5 / 共效 0.6 / 小攻 0.25，即 `凝夜白霜` 一类），
tier = 5，该只出现 k 条有效词条（其余填该套装不认的 生命/防御）；**B = 20.5**；下表为 `judge_echo` 实跑结果。

| k | 有效词条（Σw） | A | A≥B | 全最低档 | 全平均档 | 全满档 |
|---|---|---|---|---|---|---|
| 1 | 暴击（1.00） | 10.0 | ✗ | 8.40 不合格 | 10.00 不合格 | 14.00 不合格 |
| 2 | +爆伤（1.70） | 17.0 | ✗ | 14.24 不合格 | 16.95 不合格 | 23.74 **保留** |
| 3 | +共效（2.30） | 23.0 | ✓ | 18.58 不合格 | 22.83 **保留** | 31.65 达标 |
| 4 | +大攻（2.80） | 28.0 | ✓ | 22.22 **保留** | 27.71 **保留** | 38.24 达标 |
| 5 | +小攻（3.05） | 30.5 | ✓ | 23.93 **保留** | 30.00 **保留** | 41.67 达标 |

> 注意 k=5「全平均档」落在**保留**而非达标：期望值常不在档位表上（如爆伤期望 15.1，档位只有 15.0），
> `snap_to_tier` 取最近档 → `r` 略低于 1.0 → 总分 30.00 < A 30.5。也就是**"平均档"实际够不到"满分达标"**。

**过保留线（B = 20.5，本套装）所需的最低平均档位 `r̄`**：

| k | Σw | 需要 r̄ | 可行性 |
|---|---|---|---|
| 1 | 1.00 | 205% | **不可能**（上限 140%） |
| 2 | 1.70 | 121% | **不可能** → 该只的归宿是"建议重铸"或"不合格" |
| 3 | 2.30 | 89% | 需要高于最低档 |
| 4 | 2.80 | 73% | 最低档（68.6~84%）即可 |
| 5 | 3.05 | 67% | 最低档即可 |

---

## 4. 分位与完成度（阶段十七/十八）

官方概率表（`assets/echo_probability.json`）除了做期望归一化，还给出两个度量：

| 度量 | 实现 | 回答什么 | 报告呈现 |
|---|---|---|---|
| **单条分位** | `echo_stats.tier_percentile(名, 值)` = 概率表里 ≥ 该档的概率之和 | "这条在它的分布里排前几" | 词条明细尾附 `[前 X%]` |
| **完成度** | `echo_score_sim.completeness(套装, 分)` = 得分 ÷ **该套装 Top-5 权重词条全满档**的分数 | "离毕业有多远" | 得分下方 `完成度 X%`（粗体） |

**为什么整只指标是"完成度"，而不是"击败随机产出的 X%"**（后者已实现、已评估，但**不进报告**）：
`击败 X%` 以**随机产出**为基准，而它对"抽到几条有效词条"远比"档位多高"敏感 —— 一条"认的"词条贡献
`6.9w~14w` 分，而"不认的"贡献 **0**，所以"认/不认"的差距是数量级的，档位高低只在 ±40% 内浮动。
后果是"5 条全最低档"就已击败 97%、"全平均档"99.9%、"毕业"100% —— **挤成一团**，
玩家看了只会困惑（"21 分怎么击败 88.9%？"）；而玩家的比较体系本来就是"离满配/毕业有多远"。
（`score_percentile` / `score_distribution` 仍留在 `src/echo_score_sim.py` 供离线分析。）

**完成度**用"毕业"当标尺（固定分母，**同一套装内**跨件可比；各套装的权重尺度不同 —— 见 E4 —— 故**跨套装不可直接比**）。实测锚点（`流金溯真之式`，满分 = Top-5 全满档 = 43.51）：

| 样本 | 得分 | 完成度 |
|---|---|---|
| 只出 3 条有效、暴击垫底 | 21.18 | **48.7%** |
| 5 条全最低档 | 25.13 | 57.7% |
| 5 条全平均档 | 32.00 | 73.5% |
| 5 条全满档（毕业） | 43.51 | **100%** |

它同时含"有效条数"与"档位高低"：**只出 3 条有效的件，上限天然只有约 72%**（分母是 5 条）。

> 单条分位与"判定"衡量的也不是同一件事：判定 = 该套装**门槛内的绝对水平**（受套装取向影响，
> 如占位键套装门槛低 → 容易"达标"）。

（"击败 X%"这个指标曾在报告里展示过，现已按用户要求移除 —— 理由见上。它的实现
`echo_score_sim.score_percentile` 仍保留，可离线算。）

> 整只分位的自检：`E[分数] = 5 × 10 × Σw / 13`（未认词条计入 13 种的分母），实测 5 个套装偏差 < 0.12%。

---

## 5. 关键设计选择（为什么这么定）

1. **达标线为什么不看套装配置，而看"这只实际出现了什么"**
   旧规则（该套装有效键最低 n−1 条之和）与"这只出了什么"无关。极端案例：`轻云出月`（9 键，4 个专伤压到 **0.1**）旧线仅 **4.0**；`隐世回光`
   （已被用户删掉 4 个占位键，现 5 键）**19.0**；对比 5~6 键套装的 17.5~20.5 → **词条种类越多反而越容易达标**。

2. **为什么要求 `A ≥ B` 才可能达标**
   `k ≥ n−1` 时 `A ≥ B` 恒成立；`k < n−1` 时求和项少、A 可能低于 B —— 那意味着"出现的有效词条太少，
   不足以支撑达标"，该只**最高只能到"保留"**。

3. **为什么不给 A 加"通用线"下限**
   套装配置被定制即为刻意的取向（如 `隐世回光` / `轻云出月` 用极低权重"占位键"压低基准线，因为真正核心词条少）；
   用通用线兜底会覆盖作者的意图。

4. **"建议重铸"为什么用"锁 N 刷 M 的期望分"而不是"数好词条"**
   重铸是**唯一要花频整器（30 元/个）的动作**，判据必须回答"回报是否为正"。旧判据（≥2 条好词条 / 四有效打底）
   只描述纸面状态：既看不出"锁 3 条要 2 个、锁 4 条要 3 个"的成本差，也会把"锁住一条防御%"算成好底子。
   新判据把这笔投入的期望收益直接算出来（见 §2）。

5. **为什么不引入"套装价值系数"**
   "某个套装值不值得练"取决于有没有对应角色/队伍，属**用户策略**而非可推断的数值。程序保持"单套装视角"，
   用户用报告的「套装」下拉筛选即可。
   （附：**"通用分"不能当"散搭收益"** —— 通用表覆盖 13 个词条而套装模板只认 5~9 个键，是口径差异。）

6. **为什么权重按"边际收益"而不是经验值**
   第 1 代权重（爆伤 0.9 / 大攻 0.85 / 小攻 0.5）把非双暴词条系统性抬高了。按 `1 + c·d` 乘区实算边际收益后
   重排（见 §1），双暴才真正被拉开：双暴件 41 只的"击败%"中位 **94.1** vs 非双暴件 68 只 **79.7**。

---

## 6. 已评估、决定不改的项（避免重复讨论）

> 下表数字均在**当前权重表**下重跑（109 只）。

| # | 提议 | 实测后果 | 结论 |
|---|---|---|---|
| **E1** | B 线改成"最低 **n** 条之和" | 对 5 键套装，新 B **正好等于 A 的上限**（凝夜白霜 30.5 = 最高 5 条之和）→ `A ≥ B` 只在 k=5（5 个词条位全有效）时成立，"达标"退化为"必须凑齐 5 条"。分布：达标 **17** / 保留 **12** / 建议重铸 12 / 待强化 8 / 不合格 **60** —— 整体塌向"不合格"。 | **不改**。`(n−1)` 是刻意的：`A ≥ B` 的语义就是"k ≥ n−1 才放行"；改成 n 条会让 A/B 都变成"权重之和"而重合 |
| **E2** | 上调 A 线门槛到 103~108% | 达标 39 → 32/20/17，**全部平滑落到"保留"**（38 → 45/57/60），建议重铸/待强化/不合格**完全不动**。 | **不改**。"达标"的语义就是"达到平均档位（100%）"，达标变多是权重修正的自然结果，不该再人为压 |
| **E3** | 给暴击加"稳定性溢价"（满暴击 → 未暴击要重打） | 暴击 ×1.10/1.15/1.25 → 达标 40/41/41，**×1.10 后基本饱和**。 | **未默认启用**。影响极小；需要时把 `DEFAULT_WEIGHTS['暴击']` 与套装模板的暴击权重同乘一个系数 |
| **E4** | 强制权重 ≤ 1（"同尺度"约定） | 权重整体缩放**完全不影响判定**（分数、A、B 同比例）→ 只有相对比例有意义。 | **撤回该约束**。`隐世回光` 的攻% 1.0 / 共效 1.2 无害 |
| **E5** | "通用分"当"散搭收益" | 见 §5.5。 | **不做**（口径差异，不是"散搭更强"） |
| **E6** | 主属性（COST）纳入评估 | 游戏自带主词条弃置/过滤 → 能进评估的件主属性必然正确，程序再判是重复劳动。 | **不做** |
| **E7** | 手动编辑套装模板权重 | 用户会在"套装配置"里按角色微调（例如把某套装的专伤从按因子折算的 0.53 改成 0.4）。**这会改变分布**（实测 39/38/10/13/9 即该状态）。 | **以文件为准**。文档里的分布只对应"跑报告那一刻"的权重表 |

---

## 7. 未验证 / 风险

- **真机实跑未做**：全部结论基于 `logs/eval_debug/20260911_110817` 的离线重放（转录文本比实时 OCR 干净）。
  建议真机跑一次评估，核对报告的 `verdict` 分布与截图。
- **档位表版本**：`echo_stats._TIERS` 与游戏版本绑定（已与官方公示逐档核对一致，13/13）。改版需同步
  `_TIERS` 与 `assets/echo_probability.json`，否则 `r`、`A`、分位全偏。
- **概率表缺失的兜底**：`assets/echo_probability.json` 不可用时会回退算术平均（`get_mean`），此时
  "平均档 = 10 分"的语义会失真（≈ 只需 90% 算术平均）。
- **静态权重 vs 实际面板**：程序不知道你的当前面板（c / d / 增伤 X），所以权重表达的是"该阶段典型面板下
  这条词条值多少"，无法逐件判断"这条暴击是否已溢出""这条专伤是否对口"。要突破需引入"当前面板"输入。
- **占位键套装的低门槛**：`隐世回光` / `轻云出月` 的极小权重会把 B 压到很低，使"几乎任何件都过线"，
  这是**刻意的宽容**（辅助/通用套）。不在评估口径里额外约束。
- **重铸成本是估算**：`REFORGE_PRICE = 30`（元/个）仅用于报告展示；真实成本随市场波动。
- **整只分位是蒙特卡洛**：默认 5 万次/套装（分位误差约 ±0.7%，按套装缓存，首次约 1 秒）。追求更精确可调
  `echo_score_sim.SAMPLES`。

---

## 8. 附录：当前实现的离线表现（109 只）

> 数据由 `tools/offline_eval_report.py` 重放 `logs/eval_debug/20260911_110817` 得到，**以实跑为准**。

| 判定 | 数量 |
|---|---|
| 达标 | 39 |
| 保留 | 38 |
| 建议重铸 | **0** |
| 待强化 | 13 |
| 不合格 | **19** |

**"建议重铸"为 0 是阶段二十一的直接结果**：未过保留线的满级件共 13 只，其中 5 只存在"代数/期望"可行的
方案，但**真实达标概率只有 21.6% / 23.0% / 34.2% / 42.7% / 51.9%**（蒙特卡洛 3000 次），全部低于
`REFORGE_MIN_PROB = 60%` → 判"不合格"；剩下 8 只连粗筛都过不了（重铸后的达标线站不住）。

各套装贡献（达标 / 保留 / 建议重铸 / 待强化 / 不合格）：

| 套装 | 达标 | 保留 | 重铸 | 待强化 | 不合格 | Σ |
|---|---|---|---|---|---|---|
| 长路启航之星 | 7 | 7 | 0 | 5 | 1 | 20 |
| 隐世回光 | 2 | 1 | 0 | 1 | 15 | 19 |
| 雪落无声之愿 | 2 | 11 | 0 | 2 | 0 | 15 |
| 轻云出月 | 10 | 3 | 0 | 2 | 0 | 15 |
| 流金溯真之式 | 7 | 6 | 0 | 1 | 0 | 14 |
| 听唤语义之愿 | 5 | 6 | 0 | 0 | 1 | 12 |
| 息界同调之律 | 4 | 4 | 0 | 1 | 1 | 10 |
| 清邪荡煞之心 | 1 | 0 | 0 | 1 | 1 | 3 |
| 斑驳粉饰之沫 | 1 | 0 | 0 | 0 | 0 | 1 |

> `隐世回光` 的不合格最多（15）—— 它被手工删掉了 4 个占位键，基准线 B 抬到 19.0，而它的件普遍只有
> 1~2 条有效词条且档位不高。这是用户对套装的定向调整，不是缺陷。
