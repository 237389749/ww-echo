# 开发历程 | Development Log

## 阶段一：剥离 ok-ww (2026-07-07)

从 [ok-oldking/ok-wuthering-waves](https://github.com/ok-oldking/ok-wuthering-waves) 提取声骸强化模块。

**操作：**
- 复制 `EnhanceEchoTask` / `ChangeEchoTask` 及依赖链
- 删除 `char/` (52个角色)、`combat/` (战斗)、YOLO模型(37MB)、15+战斗任务
- 保留 `BaseWWTask`(1197行) → 后替换为 `BaseEchoTask`(43行)
- 项目从 ~50MB 减至 3.6MB

---

## 阶段二：评分系统搭建 (2026-07-07)

**新增：**
- `src/echo_stats.py` — 13词条档位数据 + `snap_to_tier()` / `get_mean()`
- `src/echo_set_templates.py` — 31套装模板 (JSON格式)
- 均值归一化评分: `单词条 = 档位值/均值 × 权重`
- 权重体系: 暴击2.0 / 爆伤1.5 / 攻击%1.0 / 攻击0.5 / 共效1.0 / 专属加成2.0

**策略：**
- 传统模式: 满级后一次性判断
- 渐进式模式: Lv5/Lv10/Lv15/Lv20/Lv25 逐级评估，不达标立即丢弃
- 评估模式: 只读遍历，截图+评分，生成HTML报告

---

## 阶段三：自建 UI (2026-07-08)

放弃 ok-script 原生 GUI，自建 PySide6 界面。

**UI 面板 (7 tab)：**

| Tab | 来源 |
|-----|------|
| 设备设置 | ok-script StartTab |
| 热键设置 | ok-script Game Hotkey global config |
| 强化运行 | 自建 — 模式/策略/套装/启停/状态 |
| 套装配置 | 自建 — 表格编辑词条&权重, 导入导出 |
| 调试工具 | ok-script DebugTab + 共享日志 |
| 开发者 | ok-script RunCodeTab + TemplateTab |
| 关于 | ok-script AboutTab |

**关键设计决策：**
- `ok-script` 仅用作后端引擎 (截图/OCR/模板匹配/键鼠)，不创建其GUI
- `mainui.py` 直接调用 `og.app.start_controller.start()`
- 套装词条&权重→JSON双向同步，JSON即保存即导入导出

---

## 阶段四：代码审查与修复 (2026-07-08)

双 Agent 审查 (代码 + 产品)，修复 P0/P1 共 9 项：

**P0 (4项):**
1. 评估/强化阈值不一致 → `evaluate_only` 改用 `check_echo_progressive`
2. 评估进度不显示 → 状态栏增加评估计数
3. `dev_tab` exec() 安全漏洞 → 移除 `exec()`
4. `_stop` 竞态条件 → 加 `thread.join()`

**P1 (5项):**
5. 三重命名体系 → 统一 `echo_stats` 键名为 OCR名，删除 `_OCR_TO_TIER_NAME`
6. 归一化代码重复4次 → 提取 `_normalize_stat()` + `_pair_props()` 静态方法
7. 恢复默认按钮无效 → 改名"重新加载"
8. 评估取消不删临时文件 → 加 `shutil.rmtree` 清理
9. 死代码: `is_max_level`, `score_stat`, `DEFAULT_WEIGHTS`, `get_stat_weight`, `text_white_color`

---

## 阶段五：日志/注释审查 (2026-07-08)

Agent 审查日志质量：

**修复：**
- `evaluate_only` 中 `threshold` 变量未定义 (NameError → 崩溃)
- `lock_and_esc` 日志误导 ("成功弃置" → "成功上锁")
- OCR 输出级别过高 (info → debug)
- 死代码 `src/__init__.py` 删除

---

## 阶段六：云游戏适配 (2026-09-08)

目标: 鸣潮跑在云游戏(浏览器/云客户端, 如 Firefox 全屏)时也能自动强化/评估。

**运行模式双轨 (`config.apply_run_mode`)**
- `local`(默认): exe/hwnd_class 自动匹配本地 `UnrealWindow`, PostMessage 后台交互
- `cloud`: 置空 exe/hwnd_class, `title=re.compile('鸣潮')` 让 ok-script `find_hwnd` 正则自动锁定标题含"鸣潮"的窗口(免手动选窗); interaction=Pynput(前台真实键鼠); capture 锁定 `BitBlt_RenderFull`(WGC 对云客户端不可靠); `supported_resolution.ratio=None` 跳过 16:9 校验(16:10 屏); 模式存 QSettings("OK-Echo"), 重启生效

**ok-script(site-packages)补丁 — 升级 ok-script 会丢失, 需重打(均在代码内标注 `[ww-echo ... patch]`)**
| 文件 | 改动 |
|------|------|
| `ok/gui/StartController.py` | `_auto_bring_to_front`: 仅当用户点开始(task 非 None)时, device-ready 等待循环每轮自动 `bring_to_front()`(含最小化恢复); 纯启动不抢前台 |
| `ok/device/capture_methods/hwnd_window.py` | `visible = not IsIconic()` 替代 `is_foreground()`: 后台/被遮挡窗口仍视为可用(BitBlt_RenderFull 可抓), 不再因切走而翻转状态 |
| `ok/device/interaction_methods/pynput.py` | `should_capture()`→True(任务主循环不再被前台门控); `clickable()` 保持纯判断; 新增 `_ensure_clickable()` 供各动作(click/send_key/scroll…)非前台时自动拉前再操作 |
| `ok/util/window.py` | `find_hwnd` 开头过滤空 exe 项: `exe_names = [e for e in (exe_names or []) if e]`: 云模式 `selected_exe=''` 时, 空 exe 会让所有窗口在 exe 匹配处被判不匹配 → 找不到"鸣潮"窗口 |

**踩坑记录**
- 云游戏本地无 `UnrealWindow`/`Client-Win64-Shipping.exe` → 按窗口类找目标整套失效(为什么 navigator 这类"读屏工具"能用于云游戏, 本项目"读窗口工具"不能)
- `PynputInteraction` 前台注入必须目标窗口在前台, ok-script 原逻辑把"是否前台"当运行前提 → 切走即停/一运行抢前台; 拆成"点击动作层自动拉前, 识别层不要求前台"
- 缺 `pynput` 依赖(点击层运行时才 import, 缺库表现为点击无反应) → 已补 requirements
- 鸣潮背包滚动有"热区": 光标需在网格中右部(≈归一化 x0.42-0.63), 左侧列与底部不响应; ~8px/notch
- 高 DPI 200% 下固定 860x640 逻辑窗口物理化超屏 → 初始 700x520 + 工作区 clamp/居中(`ui/main_window.py`)

---

## 阶段七：评估遍历 v2 + 词条过滤 (2026-09-09)

原 evaluate_only/run 只处理"当前选中声骸", 无"切下一只/识别/到底"三件套(原版 ok-ww 同样没有), 表现为秒退"无可评估/无可强化"或死循环同一只。

**评估遍历 v2 (`evaluate_only`)**
- 识别: OCR 网格区 `+xx` 角标 → 聚行 → 6 列基准(跨屏记忆 `col_centers`) → **行内缺列补全防漏点**
- 点格 → 右侧详情 OCR → 评分/截图/记录 → 空格/未切换**跳过继续**(不提前滚漏真实格) → 本屏处理完滚动 15 notches(光标热区 0.52,0.5)
- 到底: 详情"0 词条"(全新/列表末)结束; 左上 `声骸N/3000` 数量上限收尾; 连续 3 次滚动无新格兜底
- 唯一性: 废弃角标 dhash(同种声骸角标相同会误跳) → 用**详情全量 OCR 文本签名**

**词条筛选关键修复 (`echo_stats.is_stat_match`)**
- 详情面板属性行混排主属性与词条; 主属性数值可能落在词条档位区间内(如低等级主属性 攻击54 ∈ [30,60]) → **区间判断会误收**
- 改**离散档位集合匹配**: 数值须≈档位表中某个档位值(容差 0.8); 主属性 150/1915/54/15.1% 全部被拒; 词条最多 5 条
- 评估判定统一用"得分 ≥ tier 阈值"(1.0/2.0/2.5/3.0), 不再调 `check_echo_progressive`(避免两套阈值打架)

---

## 阶段八：评分体系与判定重构 (2026-09-11)

用户主导的评分/权重/阈值全链路重设计。

**权重标度**
- 条分改为 `档位值÷均值 ×10×权重`（每条上限自然 12.5，无效 0 分）；旧标度作废
- `DEFAULT_WEIGHTS` 定稿：暴击1.0 / 爆伤0.9 / 百分比三系(攻/生/防)0.85 / 共效0.7 / 专伤(普攻/重击/共技/共解)0.6 / 固定三系 0.5（白值700-1000下小攻击≈1%攻，全乘区通用）
- 套装模板权重多次重映射对齐通用表

**达标线(锚线)**
- `_tier_threshold` = 有效词条"平均档加权分"(10×权重)中**最低 (tier-1) 条之和**；通用 6 种适配集：Lv15=11 / Lv20=18 / Lv25=26.5；键数不足取全部键
- Lv5/Lv10 = **结构判定**：存在"首条核心词条"(`_core_first`，缺省=全部有效词条；通用 weight>0)即过，不限分

**判定统一**
- 抽出 `judge_echo(set_name,tier,score,stats)` → `(verdict, verdict_cn, threshold, keep)`，评估与渐进强化共用一份（强化 = 评估判定 + 强化动作）
- `keep`(建议保留重铸)：满级不达标 且 有效条数≥2 且 有效分≥18（洗练成本线，锁2追3 语义）；**强化流程不豁免**（不达标仍丢弃），仅评估报告标注——按用户决定不做游戏内重铸动作

**套装模板校验**
- `_core_first` 首条核心词条（套装配置 UI 有"首核"勾选列，缺省全勾=全部有效）
- **键数<5 → 评估/强化启动硬拒**（强化后必有 5 词条，最少需 5 个有效）

**报告增强 (`_build_eval_html`)**
- 判定四档：达标 / 待强化 / 建议保留重铸 / 不合格（兼容 0级）
- 名称独立列；筛选：得分区间(≥/≤) + 判定多选 + 名称包含匹配；词条按档位 ratio 着色(roll-4~0)
- 评估模式隐藏套装下拉（改为按声骸名映射，见阶段九）

**套装配置 UI**
- 勾选词条自动带出通用默认权重（专伤=0.6 等，替代固定 1.0）；权重 SpinBox 两位小数（0.85 不失真）

## 阶段九：套装声骸数据与自动映射 (2026-09-11)

**数据结构**（`echo_set_templates.json` 每套装）
- `_echoes: {"4c":[...], "3c":[...], "1c":[...]}` 该套装的声骸清单
- `_icon`: 套装图标名（wuther.in `IconElementAttri{名}`，如 Cloud/Ice）

**数据来源**（合规说明）
- kurobbs 需登录令牌、wuther.in robots.txt 明确 `Disallow` AI 爬虫 → **不做脚本爬取**；改由用户浏览器保存页面后本地解析
- 解析要点：声骸卡片左上角图标 ↔ 筛选按钮"图标+套装名"对照；一个声骸可属多套装（多图标）
- 解析工具 `tools/parse_wutherin_echoes.py <页面.html>`：只覆盖解析到数据的套装（**保留手补值**）、自动补 `_icon`/新增套、生成 `echoes_check.md` 核对表
- 结果：34 套（31 原 + 3 新：羽落空尘之歌/清邪荡煞之心/冥途夜行之灯）；后以用户手工整理的 `1.txt` 为准整体校准 `_echoes`——移除错误前缀「异相」(正确为「梦魇」)、剔除误录入的角色/多余声骸、补全 `剪心辑梦之影` 等缺失清单

**评估自动映射**
- `get_sets_by_echo(名)` → 候选套装列表；`get_set_by_echo(名, prefer=config套装)` → prefer 在候选内则尊重，否则模板顺序首个；未录入回退通用
- 评估逐格按声骸名取套装 → 用该套装的权重/有效键/首核/锚线判分（不再需要通用下拉）

---

## 阶段十：代码审查修复 + 报告着色改造 (2026-09-12)

**审查修复(11 项, P1+P2/P3)**
- 文案对齐新标度: `EnhanceEchoTask.config_description` 旧阈值描述 → 新标度
- 评估注释/日志: "固定通用权重" → "按声骸名自动映射套装权重"
- 边界/死代码清理: `src/globals.py`、`src/scene/WWScene.py`、`src/task/ChangeEchoTask.py`、`ui/dev_tab.py`、`ui/settings_tab.py`(`addItem(None)`)、`src/task/process_feature.py`(docstring)
- `mainui.py`: `config['debug']` 默认 `True` → `False`(不再默认开调试); `config.py`: `calculate_pc_exe_path` 补注释

**报告词条着色: 多色相离散 → 单色相连续渐变**
- 旧: `roll-0~4` 五档离散色(绿/黄/橙/红), 与判定列语义色冲突且色相跳变
- 新: `_ratio_color(ratio)`: `t=clamp((ratio-0.65)/0.7,0,1)`, `L=92-46*t`, 返回 `hsl(210,60%,L%)` → **越深越高档**; 词条改用 inline `style="background:…"`
- CSS 删 `roll-0~4`; `.stat` 加 `border:1px solid rgba(0,0,0,0.06)`(白底描边保证可见)

---

## 阶段十一：声骸名容错匹配 (2026-09-12, **工作区未提交**)

**背景**: 反向索引里 181 个声骸名中 **120 个属 ≥2 套装**(共 351 条归属), 加上 OCR 错字, 使"按声骸名映射套装"频繁错配/丢失。

**实现(`src/echo_set_templates.py`, 照词条过滤 `_normalize_stat` 的三级思路)**
- `_echo_chars`: 全部声骸名的汉字白名单(**341 字**), 建反向索引时顺带收集
- `_strip_echo_prefix`: 剥「异相」皮肤前缀(`异相·双极·星升辉铳` → `双极·星升辉铳`); **「梦魇·」是真实前缀(独立声骸), 保留**
- `_match_echo_name`: ①剥前缀 → ②精确 → ③逐字白名单清洗(剥错字/杂字) → ④子串(唯一候选) → ⑤LCS 最长公共子串(≥3 字且唯一); 全不中返回 `[]`
- `get_sets_by_echo` 改调 `_match_echo_name`; 返回**候选列表**(非取首个)
- **未提交**: 用户要求与"套装图标识别"一起做完再提交

**实测**
- 正确: 海维夏 / 戏猿 / 车刃镰 / 双极·渊陨重锋 / 异相·双极·星升辉铳 / 梦魇·青羽鹭
- **已知失效(名字层无解, 交图标识别兜底)**:
  - `梦魔·青羽鹭`(魇→魔 错字): 清洗后含子串「青羽鹭」而该名在索引中独立存在 → 子串分支唯一命中抢跑 → 误配 `[啸谷长风, 浮星祛暗]`(应 `[息界同调之律]`, 即「梦魇·青羽鹭」所属套); 若走 LCS 会得「青羽鹭」「梦魇·青羽鹭」两个 3 字平手 → 本应返回 `[]`
  - `侏侏驼`(驼→鸵): 清洗后仅剩 `侏侏`(2 字) → 命中 2 候选 → LCS 仅 2 字 → `[]`
  - `咔嘧` / `阿磁磁`(真名 咔嚓嚓 / 阿嗞嗞): 清洗后仅剩 1 字 → `[]`

---

## 阶段十二：套装图标识别 (2026-09-12)

**背景**: 阶段十一的名字容错仍有多义(**120/181 声骸属多套**)与错字无法消歧的案例 → 改读游戏详情面板的套装图标作为硬信号。

**输入**(视觉模型给出, 源 `vision.txt`): 图标在等级 `+25` 文字右侧, 详情面板内归一化 `(0.2100,0.1231)-(0.2535,0.1667)`(对应整帧 `(1402,199)-(1430,227)`@1920x1200); 圆形外径≈28px(含 2px 外发光); 与网格滚动无关(抽样 22/219 张波动 ≤±3px) → 可写死归一化坐标。

**方法**(`src/echo_icon_match.py`)
- 图标区 → 灰度 28x28; 模板 `assets/echo_icons/*.png`(34 个, 76x76) 缩到 29px(尺度 1.05)中心裁 28 → 两端同分辨率 1:1 比
- 判据 = **灰度 ZNCC** + **±2px 平移搜索**(消 bbox 抖动; 不搜索时 s1 从 0.9 掉到 0.23) + 圆形 mask(r≤12, 排外发光/面板背景)
- 置信线: `MIN_SCORE=0.60`(最佳 ZNCC) + `MIN_MARGIN=0.05`(与次优间隔), 不达线返回 None
- **弃用方案记录**: 视觉模型实测"34 个模板缩小后全屏搜 top1 错(愿戴荣光之旅 0.780, 真实 雪落无声之愿)" → 那是**全屏滑窗**的轮廓退化; 本实现改为**固定坐标裁剪 + 同分辨率 1:1 ZNCC** 后不再有该问题。**颜色/环色特征实测无效**(模板同构: 彩色圆环 + 白底 + 深色图案, 缩到 28px 后轮廓主导 → 环色 top1 命中仅 28/183)

**集成**(`EnhanceEchoTask.resolve_set_name`)
- 图标优先(**与名字候选不一致时也以图标为准**) → 低置信回退名字候选 → 再回退 config(评估=通用)
- 来源 `icon`/`name`/`default` 记入报告 JSON(`set`/`set_src`); 日志 `[套装图标] 名 → 套装 (s1=… margin=…)[ — 名字候选 […] 由图标纠正]`
- 评估报告(`_build_eval_html`)新增**套装列**: 显示 `set`, 悬停看来源; 旧报告缺该字段显示 `—`。
- 评估报告新增**「评估规则」折叠卡片**(`_build_rules_html`): 评分公式(档位值÷均值×10×权重, 每条上限 12.5)、
  达标线(有效词条最低 n−1 条平均档加权和; 通用 11/18/26.5; Lv5/10 结构判定)、判定四档语义、
  套装判定来源(图标 s1≥0.60/margin≥0.05 优先, 否则名字候选), 并附**本次报告涉及套装的权重表**
- 评估报告筛选区新增**套装下拉**: 选项 = 本次出现的套装 + 全部套装(带条目数、按数量降序), 精确匹配 `data-set`;
  与得分区间/判定多选/名称包含串行生效, 清除筛选时一并重置
  报告筛选由 `data-*`(data-score/data-verdict/data-name/data-set)驱动, 不依赖列索引 → 加列不影响筛选
- 新增 `cv2.imdecode(np.fromfile(...))` 读模板(中文文件名, `cv2.imread` 返回 None — 见 CLAUDE.md 已知坑)

**离线回归**(`tools/eval_icon_match.py`, 用 `logs/eval_debug/<时间戳>/` 全屏图, 无需开游戏)
- 219 张: **217 张高置信(99.1%), s1 中位 0.894**; 名字候选非空 211 张中 **196 一致(92.9%)**
- 15 张不一致**全部是名字层已实证的错字案例**, 图标给出正确套装: `梦魔·青羽鹭`/`梦魔·啾啾河豚`/`梦魔·咕咕河豚` → `息界同调之律`(s1=0.93, margin=0.43); 同套装图标的 s1 完全一致 → 匹配稳定
- 2 张低置信(`无归的谬误`, s1≈0.22): 该详情面板图标区**没有图标**(裁出来是深色背景 + 立绘边缘) → 回退名字候选 `隐世回光`(正确)

---

## 阶段十三：报告完善 + 离线重放 + 兜底核实 (2026-09-13)

**报告增强**
- 「套装」列(取 `results[].set`, 来源 `set_src` 放 `title`; 旧报告缺字段显示 `—`) + 筛选区「套装」下拉
  (选项 = 本次出现的套装 + 全部, 带条目数并按数量降序; JS 精确匹配 `data-set`, 与得分/判定/名称串行)
- 「评估规则」折叠卡片(`_build_rules_html`): 评分公式 / 达标线(通用 11/18/26.5; Lv5/10 结构判定) /
  判定四档语义(含"建议保留重铸仅报告建议") / 套装判定来源 + **本次报告涉及套装的权重表**

**离线重放工具(`tools/offline_eval_report.py`)**
- 用 `logs/eval_debug/<时间戳>/` 的整帧 + `image_report.md` 转录, 走线上同一份实现生成 `eval_report.html`
- 改评分/判定/报告渲染后可**不开游戏**核对; 实测 219 张 → 109 条(去重 102、0 级 1)

**漏条排查(结论: 离线脚本 bug, 线上无此问题)**
- 现象: 用户核对报告发现若干声骸词条数少于截图
- 根因: 转录里属性图标被 OCR 误读成前缀且与真名用 `|` 相连(`器 | 暴击伤害 | 15.0%`); 脚本按
  **第一个** `|` 切分 → 属性名=`器`、值=后续整段 → `parse_number` 得 0 → 整条被拒
- 修复: `rpartition('|')`, 前缀整段交给 `_normalize_stat` 清洗; 正向副作用: 签名一致化 → 重复记录正确合并(116→109)
- **线上不受影响**: 4071 个真实 detail box 里含 `|` 的 = 0(该形式只是转录"同行片段按 x 合并"的产物)

**兜底核实(线上三重防线, 详见 CLAUDE.md「技能行 / 主属性语义噪声」)**
- 单字误读被 `property_pattern` 的 `{2,}` 挡; `声骸技能` 被排除名单挡; 图标误读前缀被 `_normalize_stat`
  逐字白名单救回(181/1718 = 10.5%); 拿不到数值的杂行被 `is_stat_match` 的离散档位挡
- 两处语义噪声(变体 `使用声骸技能…` 进候选、属性伤害加成误归一化)实测无害, 已记录警戒条件

---

## 阶段十四：锚线改按"出现的有效词条"平均计算 (2026-09-13)

**动机**: 开词条资源紧张, 判定必须精打细算、只把"值得升级的"筛出来 —— 既不能因套装键数多而被稀释放宽。

**旧规则的缺陷(实测)**: 原 `_tier_threshold` = 套装有效键的 10×权重"**最低 (tier-1) 条之和**" →
- 轻云出月(9 键, 4 个专伤压到 0.15) Lv25 = **6.0**; 隐世回光(9 键, 生命/防御 0.1~0.15) = **5.0**
- 对比 6 键套装 26.5~28.5 → **词条种类越多反而越容易"达标"**, 与难度直觉相反

**新规则**: 锚线 = **本只声骸"出现的有效词条"(套装权重>0)的 10×权重平均值 × (tier-1)**
- 出现有效词条 0 条(整只不属于该套装) → 回退通用代表集锚线 11 / 18 / 26.5; Lv5/10 仍是结构判定(tier<=2 → 0)
- 签名改为 `_tier_threshold(set_name, tier, stats)`, `judge_echo` 负责传 stats; 评估/渐进强化同步生效

**实测影响(同一批 109 只离线重放)**: 达标 40 → **17**; 建议保留重铸 38 → **64**; 待强化 20 → 5; 不合格 18 → 23。
(达标收得最紧; 而"底子够(有效≥2 且 ≥18 分)"的池子 17+64=81 与旧 40+38=78 基本持平 → 名额分配更贴"值得升级的才进门")

**下限兜底(紧随其后)**: 只有新公式会在极端情况漏网 —— 如仅出现 1 条 0.1 权重的词条时线仅 4.0(比旧规则还松)。
故锚线最终为 **max(出现有效平均档 × (tier-1), 下限)**, 下限 = max(套装有效键最低 tier-1 条之和, 通用代表集 11/18/26.5)
(只取"旧规则(套装)"不够: 它对键多权重低的套装本身塌陷, 隐世回光仅 2.0/3.5/5.0)。实测 109 只: pass 17→13 / keep 64→67 / fail 23→24 / pending 5。

**同步**: 报告「评估规则」卡片 / README / CLAUDE.md 已改为新公式。**遗留待校准**: 套装权重仍应遵守
"同尺度 ≤1"的约定(现 隐世回光 的 攻% 权重为 1.7, 会让该套装锚线偏高)

---

## 阶段十五：五档判定 + 达标线改按"出现有效词条权重之和" (2026-09-13)

**背景**: 用户明确判定分层 —— **旧规则线是基准门槛(下限), "达标"是在其之上的再筛选**; 并要求
达标线去掉通用线兜底(套装配置被定制即刻意取向, 不该被通用标准覆盖; 未定制的套装其旧规则线本就等于通用线)。

**实现**
- 达标线 `A = max(10 × 出现有效词条权重之和, 旧规则线 B)`; 旧规则线 `B = _legacy_threshold(set_name, tier)`
  (套装有效键最低 tier-1 条之和 ×10)
- 数学依据: k(=出现有效词条数) ≥ tier-1 时 A ≥ B **恒成立**(k 元子集之和 ≥ 最小 k 元之和 ≥ 最小 tier-1 元之和);
  仅 k < tier-1 时 A 可能 < B(实测 34% 样本 k < tier-1, 其中 25 只 A 真的更低) → max 兜底
- `judge_echo` 输出五档: 满级 `≥A 达标 / ≥B 保留 / 有效≥2 且 ≥18 建议重铸 / 否则 不合格`;
  未满级 `≥B 待强化 / 否则 不合格`
- 报告新增 `hold(保留)` 档: 文案/颜色(`#009688`)/计数/筛选 checkbox + 「评估规则」卡片同步
- **保留占位键**: 隐世回光/轻云出月 的 0.1~0.15 权重是**刻意**用来压低基准线(真正核心词条少),
  不是数据缺陷 —— 但需注意"权重要么 0、要么 ≥0.5", 中间值会连带压塌基准线

**达标前提修正(用户指正)**: 达标须 **score ≥ B 且 A ≥ B 且 score ≥ A** 三条同时成立 —— 故
`_tier_threshold` 返回**纯 A**(不再取 max(A, B)), 由 `judge_echo` 判 A 与 B 的关系: **A < B 意味着
出现有效词条太少、求和项不足以支撑达标, 该只最高只能落"保留"**。修正后 A < B 的 25 只全部降级。

**建议重铸判据(两次修正后定稿)**: 先收紧为「四有效 或 k=3且avg≥105%」→ 用户指出**维度错了**:
keep 的本意(CHANGELOG 阶段八)是「洗练成本线/锁2追3」, README 亦注过「单条高档位不保留(洗 4 条不值)」
→ 它衡量的是「**胚子值不值得用重铸/频整器改造**」, 而不是「现有词条够不够多」。
**定稿判据** = 满级且未过保留线时, 满足任一: ① **已有 ≥2 条词条达/超平均档(档位值/均值 ≥1.0)**
② **四有效打底(eff≥4)**。要点: ①的「好词条」**按档位水平算、不看该套装是否认** —— 重铸后套装
适配性可以改, 所以「双暴满档 + 3 废」(2 条达平均档) 正是最好的频整器胚子; 「双暴单满」(1 条) 仍不合格。
此档天然只作用于基准线正常的套装(辅助套走「保留」档) → 无需「套装价值」字段。
实测待选区 35 只 → **建议重铸 22 / 不合格 13**(上一版 11/24); 场景复核: 双暴满档+3废=建议重铸 /
双暴单满+3废=不合格 / 双暴最低+3废=不合格。

**场景验证(满级, `_TIERS` 降序故 [0]=满档)**: `双暴满档+3废`(k=2,档位125%) → 专属套 **不合格** /
轻云出月 达标; `双暴满+攻%满`(k=3,126%) → 专属套 **保留**; `双暴满+攻%最低`(k=3,107%) → 专属套 **保留**。

**实测(109 只离线重放)**: 保留 38 / 建议重铸 12 / 达标 17 / 待强化 15 / 不合格 27

---

## 阶段十六：均值改官方概率期望 + 概率表落地 (2026-09-15)

**动机**: 官方概率公示(库洛 product_info)显示**各档位概率不等**(如暴击最低三档各 23.3333%、最高档仅 3%),
算术平均 `(最高+最低)/2` 因此**高估了均值** → `r = 1.0` 实际是"高于平均"(约 60 分位), 与"平均档 = 有价值"的口径不符。

**数据(`assets/echo_probability.json`)** — 来源: 库洛官方产品信息公示(language_ko)第 3~10 页(用户浏览器保存的
`logic.htm` 文本层解析)。词条类型**等概率** `1/(13 − 已激活或锁定条数)`; 档位概率只有 3 种形状(双暴 A 类 /
其余 8 档 B 类 / 固定攻·固定防各一); 官方合计 99.9998~100%(四位小数) → 计算时按实际合计归一化。
**交叉验证: 概率表的档位值集合与 `echo_stats._TIERS` 逐档一致(13/13 无差异)** — 档位值无需改动。

**实现(`src/echo_stats.py`)**:
- 新增 `_load_tier_probs()` / `_expected_tier()`: 加载概率表(mtime 缓存, 与 `echo_set_templates` 同套路)
- `get_mean` 语义从"算术平均"改为"**概率期望档位值**" `Σ(档位值×概率)/Σ概率`; 表缺失/解析失败/
  档位值对不上(游戏改版) → 回退算术平均。调用点 4 处(2 处 `EnhanceEchoTask` + 离线重放)不改即生效
- 连带: **"每条上限自然 12.5"不再成立** — 满档条分 = 最高档÷期望×10×w ≈ **13.1~14.0**(因词条而异),
  最低档 ≈ 6.9~8.4; **平均档仍是 10.0 分**。A 线(`score ≥ A` ⟺ 加权平均 r ≥ 1.0)公式不变, 但语义变准
- **均值统一舍入到 1 位小数**(`_compute_means`): 报告「词条明细」行直接展示它, 全精度会印出
  `7.530000630000631` 这类浮点尾巴, 且展示的数与真正参与计算的数不一致 → 舍入后为 `6.9/7.5×10×1.0=9.20`。
  顺带观感改善: 暴击最低档 8.40 / 满档 14.00(整数)。对判定无实质影响(分数变动 < 0.5%)

**实测(109 只离线重放 `logs/eval_debug/20260911_110817`)**:

| 判定 | 改版前(算术平均) | 改版后(概率期望) |
|---|---|---|
| 达标 | 17 | **39** |
| 保留 | 38 | 28 |
| 建议重铸 | 22 | 14 |
| 待强化 | 15 | 15 |
| 不合格 | 17 | 13 |

分数中位 25.85 → 27.57; r̄×10 中位 9.53 → 10.12。**达标翻倍是预期效果**: 旧口径 `r̄ = 1.0` 要求"高于平均",
新口径才是"达到期望"。若希望"达标"保持稀有, 应上调 A 线(未做, 待定)。

**同步**: README(评分系统, 示例 8.4%→7.53%) / CLAUDE.md(评分公式行) / 报告「评估规则」卡片 /
`EnhanceEchoTask.config_description` 文案 均已改为期望口径。

---

## 阶段十七：分位体系（官方概率 → 分位展示 / 好词条判据 / 整只分位） (2026-09-15)

**动机**: 概率表落地后暴露一个口径问题 —— `r ≥ 1.0`("达到期望档")在不同词条上的**实际稀缺度不同**:
暴击覆盖前 **53.33%**、暴击伤害只覆盖前 **30.00%**、其余 11 个词条前 40.78%(官方档位概率分布不同所致)。
同一句"达平均档"因词条而异 → 需要一个**跨词条可比**的质量度量, 这就是**分位**。

**实现**:
- `echo_stats.tier_percentile(名, 值)`: 该档位的分位 = 概率表里 ≥ 该档的概率之和(0~100)
- 报告「词条明细」每行尾附 `[前 X%]`(纯展示)
- 报告「得分」单元格下附 **`击败 X%`**(整只分位, 见下)
- **好词条判据换分位**: `judge_echo` 的 `hi_cnt` 从"档位值/期望 ≥ 1.0"改为"分位 ≤ `HI_PERCENTILE`(默认 25%)"

**整只分位(`src/echo_score_sim.py`, 新增)**: 按官方概率模拟"随机满级声骸"的分数分布 ——
5 条副词条**无放回**(13 种里选 5, 互不重复), 每条按官方档位概率抽档位, **套装不认的词条照样占一个词条位**
(记 0 分, 这是"5 条里有几条有用"的真实来源), 条分与 `compute_weighted_score` 同式求和。
于是任意一只的得分可换算成"击败 X% 的随机声骸"。默认 5 万次/套装(约 1 秒), 按套装缓存。
**自检: E[分数] = 5 × 10 × Σw / 13**(实测 5 个套装偏差 < 0.12%)。

**实测(109 只离线重放)**:

| 判定 | 改前(好词条 = r≥1.0) | 改后(分位 ≤ 25%) |
|---|---|---|
| 达标 | 38 | 38 |
| 保留 | 29 | 29 |
| 建议重铸 | 14 | **7** |
| 待强化 | 15 | 15 |
| 不合格 | 13 | **20** |

(只影响 keep/fail —— 只有它们用 hi_cnt; 7 只"建议重铸"因好词条门槛收紧而降为不合格)

各判定的"击败 X%"中位: 达标 96.7 / 保留 92.2 / 建议重铸 86.8 / 待强化 72.0 / 不合格 79.5。
**两者衡量的是不同东西**: 判定 = "该套装门槛内的绝对水平"(受占位键套装影响, 如隐世回光有一只击败 33% 却"达标");
分位 = "相对随机产出的位置"(跨套装可比) —— 所以 `不合格` 里也能出现击败 89% 的件。

---

## 阶段十八：权重按"满档词条的边际收益"重排 (2026-09-15)

**动机**: 第 1 代 `DEFAULT_WEIGHTS`(尺制定稿)是经验值, 把非双暴词条系统性抬高了 —— 实测爆伤 +29%、
大攻 +70%、小攻 +100%。用户指出: ①小攻恒为大攻的一半; ②爆伤在高链/命座自拐下被稀释(其主C面板
爆伤 250%、毕业 100%暴击:300%暴伤); ③专伤比大攻更小且增伤区泛滥时进一步稀释。

**方法**: 按面板实算满档词条的边际收益 —— 暴击乘区 `1 + c·d`, 故
`暴击收益 = Δc·d/(1+cd)`、`爆伤收益 = c·Δd/(1+cd)`; 攻击类按 `Δ/面板` 折算。
**爆伤的相对价值 = 2c/d**(分母抵消) —— 它随练度**回落**:

| 面板(攻击类取 基础1000/面板2000~2800) | 暴击+10.5% | 爆伤+21% | 攻%+11.6% | 小攻+60 | 爆伤/暴击 |
|---|---|---|---|---|---|
| **普通 c70% d200%** | 8.75% | 6.13% | 5.80% | 3.00% | **0.70** ← 采用 |
| 现状 c65% d250% | 10.00% | 5.20% | 4.83% | 2.50% | 0.52 |
| 毕业 c90% d300% | 8.51% | 5.11% | 4.14% | 2.14% | 0.60 |

**定值口径**: 通用表是"兜底", 按**普通链度/命座**的面板(c≈70% / 爆伤≈200%)取 —— 高练度玩家会按
自己的角色在套装模板里定制权重, 不该让兜底值去迁就毕业面板。

**权重新旧对照**:

| 词条 | 旧 | 新 | 依据 |
|---|---|---|---|
| 暴击 | 1.00 | **1.00** | 边际最高; 满暴击另有"未暴击要重打"的稳定性价值(超出期望模型) |
| 暴击伤害 | 0.90 | **0.70** | 普通练度面板的 `2c/d`; 随练度回落(毕业 0.60) |
| 攻击% / 生命% / 防御% | 0.85 | **0.50** | 被声骸套装与辅助放大 |
| 共鸣效率 | 0.70 | **0.60** | 循环刚需但有阈值、够用即溢出 → 由套装按角色调整 |
| 4 条专伤 | 0.60 | **0.40** | 比大攻更小, 且 `Δ/(1+X)` 随增伤 X 递减(X=200% 时仅值暴击 0.43) |
| 攻击/生命/防御(小值) | 0.50 | **0.25** | 恒为对应大值的 0.5 倍(比值与面板无关, 机制性结论) |

`assets/echo_set_templates.json` 的 34 套权重按**同一因子**缩放(385 个键, 无归零; 各套装内部比例保留)。

**实测(109 只离线重放)**:

| 判定 | 改前(经验权重) | 改后 |
|---|---|---|
| 达标 | 38 | **44** |
| 保留 | 29 | 41 |
| 建议重铸 | 7 | 3 |
| 待强化 | 15 | 15 |
| 不合格 | 20 | **6** |

> 注: 上表是"34 套按因子折算后、**用户手工微调之前**"的口径。用户随后在「套装配置」里按角色微调了若干权重
> (长路启航之星 / 流金溯真之式的专伤 0.53→0.4、雪落无声之愿 0.53→0.5、隐世回光删掉 4 个占位键等),
> 该状态下的分布见阶段十九。

**副作用与解释**: 整体**放松**(达标 +6、淘汰 −14)。原因是被降权的爆伤/大攻/小攻**恰好是保留门槛 B 的成分**
(B = 最低 tier-1 条权重之和) → B 降幅(约 35%)大于总分降幅 → 净放松。**已评估两条收紧路径, 均决定不改**:
① **上调 A 线门槛**(103~108%) → 达标 39→32/20/17, 且**全部平滑落到"保留"**(38→45/57/60), 建议重铸/待强化/
不合格**完全不动**, 但"达标"的语义就是"达到平均档位(100%)", 达标变多是权重修正的自然结果, 不该再人为压;
② **B 改"最低 n 条之和"** → 对 5 键套装新 B **正好等于 A 的上限**(凝夜白霜 30.5), `A ≥ B` 退化为"必须凑齐
5 条有效", 整个分布塌向不合格(达标 **17** / 不合格 **60**)。详见 `eval_rules.md` §6。

**同步**: 顺带修正 `_GENERAL_WEIGHTS`(通用代表集)与新 `DEFAULT_WEIGHTS` 不一致的问题 —— 它决定"通用"模式
(套装未映射)的 B 线, 原值仍是第 1 代 `(1.0, 0.9, 0.85, 0.7, 0.6, 0.5)` → B = 26.5, 改为
`(1.0, 0.7, 0.6, 0.5, 0.4, 0.25)` → B = 17.5(样本内无未映射件, 分布不变)。
`eval_rules.md` 已**全面重写**, 对齐本阶段口径(期望归一化 / 新权重 / 分位体系 / 重算的分类审查矩阵 /
"已评估不改的项")。

**双暴是否被拉开**: 双暴件 41 只的"击败%"中位 **94.1** vs 非双暴件 68 只 **79.7**。

**稳定性溢价(暴击 ×1.10~1.25)**: 实测对判定影响极小(×1.10 后基本饱和), 故**未默认启用**;
需要时把 `DEFAULT_WEIGHTS['暴击']` 与套装模板的暴击权重同乘一个系数即可。

---

## 阶段十九：「建议重铸」判据改为"锁 N 刷 M 的期望分" (2026-09-15)

**动机**: 「建议重铸」是**唯一要花频整器(30 元/个)的动作**, 旧判据却只描述纸面状态 ——
① 已有 ≥2 条"好词条"(官方分位 ≤ 25%) ② 四有效打底(eff ≥ 4), 任一即可。两个问题:

- **看不出成本**: 官方机制是"锁 L 条 = L−1 个频整器", 所以"四有效打底改 1 条"实际要**锁 4 条 = 3 个 = 90 元**,
  而"锁 2 条"只要 30 元 —— 同一个标签下成本差 3 倍;
- **会数错底子**: 判据①按分位算、不看套装是否认(理由是"重铸后适配性可改"), 结果一件 `听唤语义之愿` 的件
  把 **`防御百分比 12.8`(前 23.3%)** 算成了"好底子", 而"锁一条防御%"毫无意义。

**实现**(`EnhanceEchoTask.reforge_plan`): 穷举 L ∈ {1,2,3,4} × "锁哪 L 条"的所有组合

```
池      = 13 − L                                     ← 官方: 锁定的词条类型不会再出现
E_feat  = 10 × Σw(该套装认的、未被锁定的词条) ÷ 池      ← 一条随机新词的期望分
期望分  = Σ(锁定条的当前条分) + (5 − L) × E_feat
成本    = max(1, L−1) × 30 元                         ← 官方: 锁 L 条 = L−1 个; 锁 1~2 条按 1 个(用户口径)
判据    = 最优方案的"期望分 ≥ B" → 建议重铸; 否则不合格
```

`HI_PERCENTILE` 常量随之废弃(分位本身仍用于报告展示 `[前 X%]`)。报告在判定列下方直接给出方案:
**「锁 暴伤+重击+攻% · 刷 2 条 · 2 个 ≈ 60 元 · 期望 17.79 分」**。

**实测(109 只, 当前权重表)**:

| 判定 | 旧判据 | 新判据 |
|---|---|---|
| 达标 | 39 | 39 |
| 保留 | 38 | 38 |
| 建议重铸 | 6 | **10** |
| 待强化 | 13 | 13 |
| 不合格 | 13 | **9** |

> 只影响 keep/fail(其余分支在它之前 return)。新判据比旧判据略宽(6→10), 因为"锁 1 条 + 刷 4 条"只要 30 元、
> 门槛比"≥2 条好词条"低 —— 但它给出的方案是**可执行且有成本**的, 不再是"看起来像胚子"。
> **质量上反而更准**: 各判定的整只分位中位变为 达标 95.2 / 保留 87.7 / 重铸 71.5 / 待强化 65.3 /
> **不合格 16.8**(旧判据下不合格档的击败率是 79) —— 现在筛掉的确实是"频整器也救不回来"的件。

**自检**: 反例输入 `[(暴伤 21.0), (防御% 12.8), …]` → 最优方案只锁 `暴伤`, **不会锁防御%**。

**判据修正（同日，用户指出）**: 上面的"期望分 ≥ B（保留线）"仍不够 —— "花 30 甚至 60 元只买到一个
**保留**档位不值"，目标必须至少是**达标**。改为（两道门）：

```
期望档位 r̄ = [Σ(rᵢ·wᵢ)锁定 + W_new] ÷ Σw_after,   Σw_after = Σw(锁定) + W_new,
             W_new = (5−L) × Σw(可用池) ÷ (13−L)
方案须同时满足 ①  10 × Σw_after ≥ B   （重铸后的达标线 A_after 自己站得住）
              ②  r̄ ≥ 1.0              （等价于 期望分 ≥ A_after）
选法: 满足①②的方案里取成本最低(并列取期望分最高);   判据: 有 → 建议重铸, 没有 → 不合格
```

- **`r̄ ≥ 1.0` 的语义**：新刷词条的期望档位恰是 1.0（平均档），所以 `r̄ ≥ 1.0 ⟺ Σ(rᵢ−1)·wᵢ ≥ 0` ——
  **能不能达标只取决于"锁住的那部分自己是否达到平均档"**；又因 `(5−L)×E_feat = 10×W_new` 两边抵消，
  它**等价于"期望分 ≥ 重铸后的达标线 A_after"**。
- **但只有 ② 是不够的（同日第二次修正）**：`r̄ ≥ 1.0` 只保证 `score ≥ A_after`，**漏掉了"重铸后的达标线
  自己就低于基准线"的情形** —— 例如"锁 1 条低权重词条 + 刷 4 条"，`Σw_after < B/10` → `A_after < B`
  → 按规则最高只能到"保留"，**怎么刷都达不到标**。必须补条件 ①：`10 × Σw_after ≥ B`。
- **实测**：那只 `梦魔·青羽鹭` 的 **15 个锁定方案**里只有 1 个满足 ①（锁 爆伤+攻% → A_after 18.14），
  而它的 r̄ 0.955 又不满足 ② → **任何方案都救不回来，判"不合格"** —— 这正是用户
  "花 30 元只得到期望 15.17 分，那我不如去刷几个胚子"的直觉。
- **修好后的分布**：建议重铸 13 → **5**、不合格 6 → **14**；通过的都是"**锁 2 条好词条 + 刷 3 条 + 30 元**"
  这类只差最后一步的件（5/5 全部如此）。
- 报告一并给出 `r̄` 与"届时达标线 A_after"，便于核对。

---

## 阶段二十：报告主指标改为「完成度」，移除「击败 X%」 (2026-09-15)

**动机**: 报告原来在得分下方标 **`击败 X%`**（按官方概率模拟"随机满级声骸"分布后这只的分位）。
用户指出它**不符合玩家的比较体系** —— 玩家拿"满配/毕业"当标尺，而"击败随机产出"会让人迷惑：
一只"3 条有效、暴击垫底"的件只有 **21.18 分**，却显示"击败 **88.9%**"。

**根因**: `击败 X%` 对"**抽到几条有效词条**"远比"**档位多高**"敏感 —— 一条"认的"词条贡献 `6.9w~14w` 分，
"不认的"贡献 **0**（数量级差），而档位高低只让单条在 ±40% 内浮动。实测（`流金溯真之式`）：

| 样本 | 得分 | 击败随机% |
|---|---|---|
| 5 条全最低档 | 25.13 | **97.0%** |
| 5 条全平均档 | 32.00 | **99.9%** |
| 5 条全满档(毕业) | 43.51 | **100%** |

从"全最低"到"毕业"只从 97% 爬到 100% —— 区分度被压平，自然让人困惑。

**实现**（`echo_score_sim.completeness` / `max_score`）：**完成度 = 本只得分 ÷ 该套装 Top-5 权重词条全满档的分数**。
分母是**固定基准**（只取决于套装配置），所以跨件可比。实测锚点：

| 样本(满分 43.51) | 得分 | 完成度 | 击败随机%(已不展示) |
|---|---|---|---|
| 只出 3 条有效、暴击垫底 | 21.18 | **48.7%** | 88.9% |
| 5 条全最低档 | 25.13 | 57.7% | 97.0% |
| 5 条全平均档 | 32.00 | 73.5% | 99.9% |
| 5 条全满档(毕业) | 43.51 | **100%** | 100% |

它同时含"有效条数"与"档位高低" —— **只出 3 条有效的件上限天然约 72%**（分母是 5 条），所以上面那只
"48.7% 连 5 条全最低档(57.7%)都不如"，与直觉一致。

**报告**: 得分下方只留 **粗体 `完成度 X%`**；**`击败 X%` 按用户要求不再展示**（`score_percentile` /
`score_distribution` 仍留在 `src/echo_score_sim.py` 供分析用，只是不进报告）。规则卡片的说明同步改写
（顺带修掉那里**还写着旧重铸判据**的过期文案）。

---

## 阶段二十一：「建议重铸」改用蒙特卡洛达标概率 + 判定单测 (2026-09-15)

**问题（阶段十九判据的缺陷）**: 「锁 N 刷 M 的期望分 ≥ 重铸后达标线」只保证约 50% 把握 —— 期望是均值，
而分数分布右偏（档位有上限、类型无放回）→ 真实概率更低。**而胚子可无限刷**：花 30 元买两三成把握不值。
实测（离线 109 只）：原本"期望达标"的 5 只，真实达标概率仅 **51.9 / 42.7 / 34.2 / 23.0 / 21.6%**。

**改动**
- `reforge_plan` 加第三道门 **蒙特卡洛达标概率 ≥ `REFORGE_MIN_PROB`(0.60)**（3000 次/方案、固定种子可复现）；
  为保证性能用三层过滤：代数粗筛 `10×Σw_after ≥ B` → 期望粗筛 `r̄ ≥ 1.0`（P≥50% 必过，故不漏）→ 精确模拟。
  选法改为"**成本最低**（并列取概率最高）"；`plan` 新增 `p_pass` / `trials`
- 报告与面板文案同步三道门；报告的 reforge 行改为显示「锁哪些 · 刷几条 · 约多少元 · **达标概率 X%** ·
  期望分 · r̄ · 届时达标线」
- 文案统一：`tier ≤ 2` 的 `verdict_cn` 由「不达标」改为「**不合格**」
- **新增 `tests/test_judge_rules.py`（12 用例）**：把 `eval_rules.md` §3 分类矩阵固化为断言，用 `mock`
  固定套装权重 → 不依赖模板文件与游戏数据。覆盖：`B(通用)` = 6.5/11.5/17.5、`A = 10×Σ出现有效权重`、
  `A` 不取 max、k≤2 不能达标、`tier ≤ 2` 结构判定、`reforge_plan` 的"<2 条返回 None"/"方案概率 ≥ 阈值"/
  "锁低权重词条被①过滤"
- **`eval_rules.md` 文档一致性**：§8 换为实测数据（建议重铸 **0** / 不合格 **19** + "为 0"的原因）并重写
  交叉表；§5.1 修正过时数值（`轻云出月` 0.1 → B=4.0；`隐世回光` 现 5 键 / B=19.0）；§4 完成度改为
  "**同一套装内**可比"；§1 标注共效 0.6 属用户口径/体验判断

**效果**：建议重铸 5 → **0**；不合格 14 → **19**（达标 39 / 保留 38 / 待强化 13 不变）

**验证**：`python -m unittest discover -s tests` → Ran **34** tests, OK（22 原有 + 12 新增，1 个按设计 skip）；
离线报告端到端跑通（约 8s，判定分布与文档 §8 一致）

---

## 当前架构

```
ww-echo/
├── mainui.py              ← 唯一入口 (PySide6); 启动先按 QSettings run_mode 调 apply_run_mode
├── config.py              ← ok-script 配置 + MODE_LOCAL/MODE_CLOUD + apply_run_mode()
├── ui/                    ← 自建UI (7 tab)
│   ├── main_window.py / run_tab.py / set_config_tab.py
│   ├── settings_tab.py / hotkey_tab.py   (settings_tab: 运行模式切换 + 窗口/截图/交互)
│   ├── debug_tab.py / dev_tab.py / about_tab.py
├── src/
│   ├── echo_stats.py      ← 词条档位 & 评分引擎 (+ is_stat_match 离散档位过滤)
│   ├── echo_set_templates.py ← JSON模板 加载/校验 + 声骸名容错匹配
│   ├── echo_icon_match.py  ← 套装图标识别(灰度 ZNCC 模板匹配; 名字消歧硬信号)
│   └── task/
│       ├── EnhanceEchoTask.py  ← 强化 run() + 评估 evaluate_only v2 遍历
│       └── BaseEchoTask.py     ← 轻量基类
├── tools/
│   ├── parse_wutherin_echoes.py ← 从保存的 wuther.in 页面解析套装声骸清单(保留手补)
│   ├── eval_icon_match.py      ← 套装图标识别离线回归(eval_debug 数据集, 无需开游戏)
│   └── offline_eval_report.py  ← 离线重放评估(eval_debug 素材) → 生成 eval_report.html
└── assets/
    ├── echo_set_templates.json ← 套装模板 + _core_first/_echoes(4c/3c/1c)/_icon (34套)
    ├── echo_icons/            ← 34 个套装图标(76x76, 文件名 = 套装名)
    └── coco_annotations.json   ← 模板匹配特征
```

## 待实现

- [x] 满级保留/重铸机制(锁2追3+高档位+重铸建议列三合一): **评估侧已实现**(keep: 满级不达标且≥2条有效且≥18分 → 报告第四档"建议保留重铸"); 强化不豁免; 按用户决定不做游戏内重铸动作
- [x] 副C/奶双逻辑评价: **已解决**——由套装模板"有效词条+权重"方案覆盖(奶套预设共效/生命/固定攻权重、双暴降权)
- [x] 剪心辑梦之影 套装的 4C/3C/1C 声骸清单: **已补**——以 `1.txt` 为准(4C 无铭探索者 / 3C 迷胧幻蛾·莳植机麋·矿岩机麋·重工铁蹄 / 1C 矿岩熊蜂·莳植熊蜂·冰盈舞者)
- [x] 声骸名容错匹配: **已实现(阶段十一)**; 已知失效(错字丢字→`[]` 或子串抢跑误配)由套装图标识别兜底
- [x] **套装图标识别**: **已实现(阶段十二)** —— `src/echo_icon_match.py` 灰度 ZNCC + ±2px 平移搜索 + 圆形 mask, 置信线 0.60/0.05; `resolve_set_name` 图标优先映射(**与名字候选不一致时也以图标为准**); 离线回归(219 张)高置信 99.1% / 名字口径一致 92.9%, 15 张名字层错字全部由图标纠正; 回归工具 `tools/eval_icon_match.py`
